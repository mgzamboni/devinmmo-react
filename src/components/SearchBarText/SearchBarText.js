import React from "react";
import { StyledSearchInfoText } from "./SearchBarText.styles";

export const SearchBarText = ({ text }) => {
  return (
      <StyledSearchInfoText>{text}</StyledSearchInfoText>
  );
};
